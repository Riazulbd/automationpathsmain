// The blog is off until Supabase is configured. Set BLOG_ENABLED=true (and
// VITE_BLOG_ENABLED=true for the frontend build) to turn it back on.
export const BLOG_ENABLED = process.env.BLOG_ENABLED === "true";
